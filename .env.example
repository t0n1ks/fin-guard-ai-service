PORT=8001
HOST=0.0.0.0
BRAIN_API_KEY=changeme_shared_secret
LOG_LEVEL=info
# Optional: Neon PostgreSQL connection string for persistent Tamagotchi state.
# When unset the service falls back to a local JSON file (fine for development).
# DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require
